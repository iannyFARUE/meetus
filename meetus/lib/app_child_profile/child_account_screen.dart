import 'package:flutter/material.dart';

class ChildAccountScreen extends StatelessWidget {
  const ChildAccountScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Child Account Screen'),
      ),
    );
  }
}