import 'package:flutter/material.dart';

class TermReportsScreen extends StatelessWidget {
  const TermReportsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Term Reports Screen'),
      ),
    );
  }
}