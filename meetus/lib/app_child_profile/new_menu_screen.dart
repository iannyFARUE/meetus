import 'package:flutter/material.dart';

class NewMenuScreen extends StatelessWidget {
  const NewMenuScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('New Menu Screen'),
      ),
    );
  }
}